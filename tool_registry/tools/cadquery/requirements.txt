cadquery>=2.4.0
aiohttp>=3.9.0
# MET-740 Phase 2: headless physics-stability smoke test for exported
# URDF robot descriptions (see physics_check.py's module docstring) --
# CPU-only, no GPU, no target-simulator install needed.
pybullet>=3.2.0
